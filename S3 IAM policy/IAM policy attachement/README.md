# Attach existing IAM policy to an existing user

The script attaches IAM policy to a user one at a time.

User will be prompted with AWS prfile (credentails), IAM username to attatch policy and and IAM policy arn to be attached.